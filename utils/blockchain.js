const Block = require("./block");

class Blockchain {
    constructor() {
        this.chain = [
            /* TODO: Create the genesis block here */
        ];
    }
}

module.exports = Blockchain;
